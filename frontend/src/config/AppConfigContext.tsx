// Arquivo legado: mantido para compatibilidade caso algum import antigo exista.
// Exporta somente componentes para não quebrar a regra react-refresh/only-export-components.
export { AppConfigGate, AppConfigProvider } from './AppConfigProvider'
