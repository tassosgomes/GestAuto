export function AdminPage() {
  return (
    <div style={{ padding: 16 }}>
      <h1>Admin</h1>
      <p>Página placeholder (visível apenas para ADMIN quando RBAC for implementado).</p>
    </div>
  )
}
